"""Payments router — placeholder for future payment management endpoints."""
from fastapi import APIRouter

router = APIRouter()

# Future endpoints:
# POST /payments/{order_id}/confirm  — record cash/card payment by staff
# GET  /payments/{order_id}          — fetch payment status for an order
