# Cart state is stored entirely in Redis; no DB table is needed.
# This file exists for structural consistency with other modules
# and serves as the place to add persistent cart models in future
# phases (e.g. saved carts, order snapshots).
